#include <string>
#include <iostream>
#include "str_new.h"
using namespace std;

int ft_len(string str)
{
    int len;
    for(len = 0; str[len] != '\0'; len++)
    {
        len;
    }

    return len;
}
